version: "3.9"
services:
  db:
    image: postgres:13.4
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: turbo
    ports:
      - "5432:5432"
    volumes:
      - "./server/turbo.sql:/docker-entrypoint-initdb.d/1.sql"
